# Productive API base URL. No token here: credentials are entered on the login screen
# and kept in localStorage only (ADR-0004).
VITE_API_BASE_URL=https://api.productive.io/api/v2

# Set to true to run the app against MSW handlers instead of the real API (pnpm dev:mock).
VITE_ENABLE_MSW=false
