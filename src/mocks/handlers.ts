import type { RequestHandler } from 'msw';

/**
 * Empty until Phase 4 records real Productive responses into `docs/api/samples/`
 * (ADR-0003: fixtures are recorded, not invented).
 */
export const handlers: RequestHandler[] = [];
